package org.firstinspires.ftc.teamcode.customclasses.preILT.mechanisms;

public enum MechanismState {
    // A enum that all the mechanisms share to have all the states in one place
    // Remade this to keep old mechanism states (in separate file) while also allowing us to create new ones
    OPEN,
    CLOSED
}
